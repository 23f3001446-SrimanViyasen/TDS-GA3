name: CI  # Name your workflow for the badge

on:
  push:     # Runs on code pushes
    branches: [ main ]  # Or your default branch
  workflow_dispatch:  # Lets you trigger manually (key for testing)

jobs:
  test:
    runs-on: ubuntu-latest  # Free GitHub server
    steps:
      - uses: actions/checkout@v4  # Grabs your code
      - name: Hello World
        run: echo "Workflow passed!"  # Simple success step
